[project]
name = "led-matrix-dashboard"
version = "0.1.0"
description = "Web dashboard for Raspberry Pi LED matrix display"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
]

[tool.uv]
package = false
system-site-packages = true
